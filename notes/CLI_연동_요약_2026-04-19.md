# CLI 연동 요약 - 2026-04-19

## Claude Code
- `claude mcp add --transport stdio --scope project master-of-hwp -- /Users/moon/Desktop/master-of-hwp/scripts/run_mcp_server.sh`

## Codex CLI
- `~/.codex/config.toml`에 `mcp_servers.master_of_hwp` 추가

## OpenCode CLI
- `~/.config/opencode/opencode.json`의 `mcp.master-of-hwp`에 local server 추가

## 현재 권장
가장 먼저 Claude Code에 연결해서 바로 tool 호출을 확인하는 것이 좋다.
